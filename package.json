{
  "name": "video-audio-web",
  "private": true,
  "version": "0.2.1",
  "description": "Single-file offline browser tool for extracting video audio tracks and converting them to MP3 with FFmpeg WebAssembly.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-standalone.mjs"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10"
  },
  "engines": {
    "node": ">=22"
  }
}
